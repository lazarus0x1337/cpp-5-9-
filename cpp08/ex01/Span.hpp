#pragma once

class Span
{
private:

public:
    Span();
    Span(unsigned int N);
    Span(const Span &b);
    Span& operator=(const Span &b);
    ~Span();
    void addNumber(int num);
    shortestSpan()
    longestSpan()
};
